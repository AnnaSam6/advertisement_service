version: '3.8'

services:
  advertisement-service:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/advertisements.db
    restart: unless-stopped
